name: verify
on:
  push:
    branches: [dev]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git status --short
add-verification-guidanc: bootstrap
run-the-full-pester-matr: core-build-out
github-actions: core-build-out
powershell: publication
github-actions: publication
string: publication
cache-dependencies-for-f: publication
normalize-python-setup-a: publication
parallelize-matrix-execu: publication
