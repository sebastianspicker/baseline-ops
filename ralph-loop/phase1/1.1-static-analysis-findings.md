# Phase 1.1 -- Static Analysis Findings

> Generated: 2026-03-21
> Scope: all `.ps1` in `scripts/` and `tools/`, all `.psm1` in `lib/`
> Exclusions applied: PSScriptAnalyzerSettings.psd1 (PSAvoidUsingWriteHost, PSUseApprovedVerbs, PSUseSingularNouns, PSUseShouldProcessForStateChangingFunctions, PSShouldProcess, PSAvoidUsingEmptyCatchBlock, PSAvoidDefaultValueSwitchParameter, PSUseBOMForUnicodeEncodedFile, PSAvoidUsingWMICmdlet)
> Items already fixed in iterations 1-5 are excluded.

---

## Summary

| Severity | Count |
|----------|-------|
| Critical | 0 |
| High | 6 |
| Medium | 14 |
| Low | 13 |
| Info | 8 |
| **Total** | **41** |

---

## Critical

_(none)_

---

## High

| # | File | Line(s) | Description |
|---|------|---------|-------------|
| H1 | `scripts/06-UpdateHealth-SSU-Proof.ps1` | entire file | **Missing `Set-StrictMode`**. This is the only numbered runtime script that never calls `Set-StrictMode`. Uninitialized variable references silently return `$null` instead of throwing, which can mask logic bugs in the update-health checks. |
| H2 | `scripts/11-IOC-Sweep-Defender.ps1` | entire file | **Missing `Set-StrictMode`**. An IOC sweep script operating on security-sensitive data should enforce strict mode to prevent silent variable typos from producing false negatives. |
| H3 | `scripts/13-LSASS-CG-HVCI-VBS.ps1` | entire file | **Missing `Set-StrictMode`**. This script audits and remediates Credential Guard / VBS / LSASS protection settings. Missing strict mode could allow misspelled registry variable names to silently evaluate to `$null`, skipping security checks. |
| H4 | `scripts/00-Run-Profile.ps1` | 229 | **`$null` on right side of comparison**: `$LASTEXITCODE -eq $null`. When `$LASTEXITCODE` is an array (unlikely but defensive), this silently returns `$false` for every element. PSScriptAnalyzer rule `PSPossibleIncorrectComparisonWithNull` would flag this. Should be `$null -eq $LASTEXITCODE`. |
| H5 | `scripts/00-Run-Profile.ps1` | 175, 227, 236, 242, 259 | **`Write-UiLine -ForegroundColor` instead of `-Style`**. `Write-UiLine` accepts `-Style` (aliased to `-ForegroundColor`), but 5 call sites pass literal `[ConsoleColor]` names like `DarkGray`, `Cyan`, `Yellow` via `-ForegroundColor`. While the alias works, it bypasses the `Resolve-UiColor` normalization layer and creates inconsistency with the rest of the codebase that uses `-Style`. |
| H6 | `scripts/04-OfficeBrowser-Hardening-Proof.ps1` | 164 | **`$InformationPreference = 'Continue'` set unconditionally after `$Quiet` handling**. Line 164 overrides the `$InformationPreference = 'SilentlyContinue'` set on line 157 when `$Quiet` is true. This means `-Quiet` has no effect on `Write-Information` output in this script. |

---

## Medium

| # | File | Line(s) | Description |
|---|------|---------|-------------|
| M1 | `scripts/01-ASR-Defender-Allowlist.ps1` | 438 | **Redefined `Write-ConsoleSummary`** shadows `Console.psm1::Write-ConsoleSummary`. 21 scripts define their own local `Write-ConsoleSummary` function. While not a runtime error (local scope wins), it defeats the purpose of the shared `lib/Console.psm1` module and makes bulk refactoring fragile. Scripts affected: 01, 03, 04, 05, 15, 18, 19, 21, 22, 25, 27, 29, 31, 33, 34, 35, 36, 38, 41, 42, 43. |
| M2 | `scripts/04-OfficeBrowser-Hardening-Proof.ps1` | 216 | **Redefined `Test-IsAdmin`** duplicates `lib/Common.psm1::Test-IsAdmin`. Also duplicated in `scripts/05-WUFB-Proofing.ps1:217`, `scripts/06-UpdateHealth-SSU-Proof.ps1:165`, `scripts/07-ScheduledTasks-Hygiene.ps1:188`. Four scripts carry their own copy instead of importing from `Common.psm1`. |
| M3 | `scripts/04-OfficeBrowser-Hardening-Proof.ps1` | 229, 283 | **Redefined `Ensure-Key` and `Save-Json`** duplicate `lib/Registry.psm1::Ensure-RegistryKey` and `lib/Serialization.psm1::Save-Json`. Also duplicated in `05-WUFB-Proofing.ps1`, `06-UpdateHealth-SSU-Proof.ps1`, `07-ScheduledTasks-Hygiene.ps1`, `11-IOC-Sweep-Defender.ps1`, `12-Suspicious-Artifact-Grabber.ps1`, `15-HardwareTPM-Audit.ps1`, `20-MissingPatch-Notification.ps1`, `31-PowerShell-Logging-Baseline.ps1`, `39-CredentialGuard-VBS-AuditRemediate.ps1`. |
| M4 | `scripts/22-SMB-Encryption-Enforcer.ps1` | 194 | **Redefined `Ensure-Cmdlet`** duplicates `lib/External.psm1::Ensure-Cmdlet`. Also in `23-BitLocker-Operations-Audit.ps1:164`, `27-Defender-Health-Audit.ps1:122`, `29-Network-Config-Audit.ps1:100`, `32-Firewall-Logging-Audit.ps1:117`, `44-Defender-Ransomware-NetworkProtection-AuditRemediate.ps1:115`. Six scripts carry local copies. |
| M5 | `scripts/10-SupportBundle-Parser.ps1` | 336 | **Redefined `Ensure-Directory`** duplicates `lib/Common.psm1::Ensure-Directory`. Also in `35-Storage-Reliability-Audit.ps1:187`, `42-Client-SecurityBaseline-Report-IntuneRef.ps1:98`. |
| M6 | `scripts/14-SecureRemoteAccessGuardrails.ps1` | 221 | **Redefined `Ensure-DirectoryForFile`** duplicates `lib/Common.psm1::Ensure-DirectoryForFile`. Also in `20-MissingPatch-Notification.ps1:196`. |
| M7 | `scripts/33-AdvancedAuditPolicy-Audit.ps1` | 89 | **Redefined `Get-FindingStats`** duplicates `lib/Console.psm1::Get-FindingStats`. The local version uses a different severity bucketing logic, which can produce inconsistent stats if both are used. |
| M8 | `scripts/08-WinGet-SelfHeal.ps1` | 204 | **Redefined `Get-StatusColor`** duplicates `lib/Console.psm1::Get-StatusColor`. Also in `18-Firewall-Baseline.ps1:184`, `17-Sysmon-Rule-Drift-Sensor.ps1:267`, `23-BitLocker-Operations-Audit.ps1:263`. |
| M9 | `lib/JsonCatalog.psm1` | 15 | **Verbose `[Parameter(Mandatory = $false)]`** on `Read-JsonFileSafe -Path`. This was identified as M1 in iteration 1 and fixed in `lib/EventLog.psm1`, but the same pattern was missed in `JsonCatalog.psm1`. |
| M10 | `scripts/29-Network-Config-Audit.ps1` | 43, 47, 50, 54, 57 | **Five verbose `[Parameter(Mandatory = $false)]`** attributes on script parameters. Same pattern as M9. |
| M11 | `scripts/36-Backup-Readiness-Audit.ps1` | 37, 40 | **Two verbose `[Parameter(Mandatory = $false)]`** attributes on script parameters. |
| M12 | `scripts/12-Suspicious-Artifact-Grabber.ps1` | 167 | **`$InformationPreference = 'Continue'` set unconditionally** after `$Quiet` check, same issue as H6. When `-Quiet` is used, this override negates the silencing. |
| M13 | 14 scripts | various | **Inconsistent `Set-StrictMode` version**. 14 scripts use `Set-StrictMode -Version 2.0` while 34 scripts use `Set-StrictMode -Version Latest` and 4 scripts use `Version 3.0`. `Version 2.0` misses PS 3+ checks (e.g., referencing non-existent properties on non-object types). The inconsistency means bug detection varies by script. Scripts using 2.0: 02, 04, 05, 07, 08, 12, 14, 15, 19, 21, 26, 27, 30, 32. |
| M14 | `scripts/dev/fix-placeholders.ps1`, `scripts/dev/fix-error-handling.ps1` | entire file | **Missing `Set-StrictMode`**. Both dev scripts omit `Set-StrictMode` entirely. While dev-only, they still manipulate source files and should enforce strict mode. |

---

## Low

| # | File | Line(s) | Description |
|---|------|---------|-------------|
| L1 | `scripts/00-Copy-Local.ps1` | 100 | **Local `Invoke-Git` shadows `lib/External.psm1::Invoke-Git`**. The script defines its own single-purpose `Invoke-Git` helper that takes `[string[]]$GitArgs` instead of the lib version's `[string[]]$Arguments`. This works because `External.psm1` is not imported, but creates a naming collision if it ever is. |
| L2 | `scripts/00-Validate-Profile.ps1` | 85 | **Local `Has-Property` defined but `lib/` has no equivalent export**. Also defined in `00-Run-Profile.ps1:65`. These two scripts define identical `Has-Property` helper functions. This is a candidate for extraction to a shared module. |
| L3 | `scripts/00-Run-Local.ps1` | 252 | **`Write-UiLine -ForegroundColor DarkGray`** uses named alias parameter; the preferred approach in this codebase is `-Style 'Muted'` which routes through `Resolve-UiColor`. |
| L4 | `scripts/04-OfficeBrowser-Hardening-Proof.ps1` | 906, 908, 926, 951, 954 | **`-ForegroundColor DarkCyan` and `DarkYellow` used with `Write-UiLine`**. `DarkCyan` is not in `Resolve-UiColor` mapping (it maps to `Cyan`). `DarkYellow` is not a valid `[ConsoleColor]` value; PowerShell will throw on non-Windows or strict console. Should use `-Style` tokens. Also `25-WinGet-Config-Baseline-Runner.ps1:337` uses `DarkYellow`. |
| L5 | `scripts/00-Copy-Local.ps1` | 38 | **Hardcoded path `C:\install\mdm\ps1`** as parameter default. Also in `00-Run-Batch.ps1:15`, `00-Run-Local.ps1:68`, `00-Run-Profile.ps1:19`, `tools/Launcher-GUI.ps1:22`. While these are parameter defaults (overridable), the path appears 5 times. A shared constant or environment variable would improve maintainability. |
| L6 | `scripts/07-ScheduledTasks-Hygiene.ps1` | 301-303 | **Hardcoded `C:\Windows\`, `C:\Program Files\`, `C:\Program Files (x86)\`** used as trusted path prefixes for scheduled task allowlisting. Should use `$env:SystemRoot` and `$env:ProgramFiles` / `${env:ProgramFiles(x86)}` for portability across Windows installations on non-C: drives. |
| L7 | `scripts/16-Sysmon-Config-Updater.ps1` | 268-271 | **Hardcoded `C:\Windows\Sysmon64.exe`, `C:\Windows\Sysmon.exe`, `C:\Program Files\Sysmon\*`** as default Sysmon paths. Should use `$env:SystemRoot` and `$env:ProgramFiles`. |
| L8 | `scripts/08-WinGet-SelfHeal.ps1` | 328 | **Hardcoded `C:\Program Files\WindowsApps`** for WinGet discovery fallback. Should use `$env:ProgramFiles` or `$env:ProgramW6432`. |
| L9 | `tools/Launcher-GUI.ps1` | entire file | **Missing `Set-StrictMode`** and **missing `$ErrorActionPreference = 'Stop'`**. The GUI launcher is the only tool that omits both. While it is user-facing GUI code, strict mode would catch typos in form control references. |
| L10 | 19 scripts | various | **95 empty `catch { }` blocks across 19 scripts** (excluded from PSAvoidUsingEmptyCatchBlock). While the analyzer rule is disabled repo-wide, many of these are not annotated with `<# best-effort #>` or similar comments explaining why the error is intentionally swallowed. This makes it hard to distinguish intentional suppression from accidental error-eating. Most problematic scripts: `12-Suspicious-Artifact-Grabber.ps1` (23), `03-LocalAdmins-Guardrail.ps1` (13), `02-LAPS-Hygiene.ps1` (11). |
| L11 | `scripts/00-Run-Local.ps1` | 252 | **`Write-UiLine -Text ... -ForegroundColor DarkGray`** uses both `-Text` alias and `-ForegroundColor` alias in the same call. Rest of codebase predominantly uses positional or `-Message` for the text and `-Style` for coloring. Minor inconsistency. |
| L12 | `scripts/31-PowerShell-Logging-Baseline.ps1` | 145 | **Local `Set-RegString`** shadows `lib/Registry.psm1::Set-RegString` with a completely different signature (the local one is a thin wrapper around `Set-ItemProperty`, while the lib version has full error handling and validation). If `Registry.psm1` is later imported, the local definition silently wins. |
| L13 | `scripts/00-Copy-Local.ps1` | 100 | **`Invoke-Git` function defined without `[CmdletBinding()]`**. Multiple script-local functions across the codebase lack `[CmdletBinding()]` (e.g., `Add-RunError` in 21, `StartsWithAny` in 07, `SB_*` functions in 09). While PSScriptAnalyzer does not enforce this in all cases, `[CmdletBinding()]` enables `-ErrorAction`, `-Verbose`, etc. on helper functions. |

---

## Info

| # | File | Line(s) | Description |
|---|------|---------|-------------|
| I1 | 26 scripts | various | **201 uses of `Write-UiLine -ForegroundColor` vs. 53 uses of `Write-UiLine -Style`**. The `-ForegroundColor` alias works, but the `-Style` parameter is the canonical approach that routes through `Resolve-UiColor` semantic tokens. The codebase is ~79% using the alias form. This is not a bug but a style inconsistency that makes it harder to change color schemes centrally. |
| I2 | `scripts/09-SupportBundle.ps1` | 172-920 | **`SB_` prefixed functions** (30 functions like `SB_WriteUi`, `SB_EnsureDir`, `SB_SaveJsonFile`) use a non-standard naming convention. All other scripts use standard PowerShell `Verb-Noun` naming. This script predates the lib consolidation and has its own parallel utility layer. |
| I3 | `scripts/04-OfficeBrowser-Hardening-Proof.ps1` | 1040 | **Commented-out pipeline output `# $allSafe`**. Dead code left from development. |
| I4 | `scripts/00-Copy-Local.ps1` | 204-207 | **Three trailing blank lines** at end of file. Minor formatting noise. |
| I5 | `scripts/45-WEF-Client-Forwarding-Readiness-Audit.ps1` | 93 | **`$ErrorActionPreference = 'Continue'`** instead of `'Stop'` used by all other scripts. This is intentional for this audit script (it collects partial data even on errors), but is undocumented and deviates from the repo convention without a comment explaining why. |
| I6 | `scripts/00-Run-Batch.ps1` | entire file | **No Bootstrap import** -- does not dot-source `_lib/Bootstrap.ps1` or import any lib modules. It constructs a temp profile JSON and delegates to `00-Run-Profile.ps1`. This is architecturally fine but means it cannot use any lib helpers for validation or output. |
| I7 | `lib/Output.psm1` | 473, 482 | **`Write-Rule` and `Write-ConsoleRule`** are functionally identical (both forward to `Write-UiRule` with the same defaults). `Write-Rule` is also defined in `lib/Console.psm1` with a different implementation. This creates ambiguity when both modules are imported (last import wins). |
| I8 | `lib/Console.psm1` | 187-202 | **`Write-Rule` in Console.psm1** has a different signature and implementation than `Write-Rule` in `Output.psm1:473`. Both are exported. When a script imports both modules, the last-imported module's `Write-Rule` wins silently. |
