# Phase 2.3 -- Convention Alignment Progress

Date: 2026-03-21

## Summary

Aligned 51 scripts to v2 conventions based on Phase 1.4 audit findings.
All changes are convention scaffolding only -- no business logic was modified.

### Test Results

- verify.ps1: PASS (parse + PSScriptAnalyzer clean)
- Pester: 329 passed, 0 failed, 24 skipped

---

## C6 -- ErrorActionPreference 'Stop' (10 scripts)

**Status: COMPLETE (10/10)**

| Script | Change |
|--------|--------|
| 02-LAPS-Hygiene.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 06-UpdateHealth-SSU-Proof.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 10-SupportBundle-Parser.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 13-LSASS-CG-HVCI-VBS.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 14-SecureRemoteAccessGuardrails.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 15-HardwareTPM-Audit.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 16-Sysmon-Config-Updater.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 17-Sysmon-Rule-Drift-Sensor.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 19-Software-Audit.ps1 | Added `$ErrorActionPreference = 'Stop'` |
| 45-WEF-Client-Forwarding-Readiness-Audit.ps1 | Changed from `'Continue'` to `'Stop'` |

---

## C11 -- Exit Codes (40 scripts)

**Status: COMPLETE (40/40)**

Added `exit 0` at the end of 40 scripts that lacked explicit exit codes.
For 00-Run-Local.ps1, propagates child exit code via `$LASTEXITCODE` instead of unconditional `exit 0`.

Scripts with existing proper exit logic were left unchanged:
- 00-Report-Aggregate, 00-Run-Batch, 00-Run-Profile (already `exit 0`)
- 02-LAPS-Hygiene, 04-OfficeBrowser-Hardening-Proof, 08-WinGet-SelfHeal (conditional exit 0/1)
- 11-IOC-Sweep-Defender (`exit $exitCode`)
- 13-LSASS-CG-HVCI-VBS (`exit $result.ExitCode`)
- 25-WinGet-Config-Baseline-Runner (`exit $finalExitCode`)
- 43-AppControlForBusiness-Audit (already compliant)

---

## C10 -- Findings Pattern (1 script converted)

**Status: PARTIAL (24/51 now use New-FindingsList/Add-Finding, up from 23)**

Converted 40-AddedLSAProtection-RunAsPPL-AuditRemediate.ps1:
- Replaced `New-Object 'System.Collections.Generic.List[object]'` with `New-FindingsList`
- Replaced 3x `.Add([pscustomobject]@{...})` with `Add-Finding`
- Added `Import-Module Results.psm1`

The remaining 20 C10-failing scripts use drift/notes/string-based patterns
(not Code/Severity/Message structured findings) and would require business
logic changes to convert. These were skipped per instructions.

---

## C9 -- Output Contract (44 scripts)

**Status: COMPLETE (48/51, up from 4; 3 N/A orchestration scripts)**

Added the v2 output contract to all 44 feature scripts (01-45 except 43):
1. `Import-Module Serialization.psm1`
2. `New-V2ResultObject` using each script's existing findings/summary/metadata
3. `Write-ResultObject` for Json/Csv output routing
4. `if ($PassThru) { $v2Result }` for pipeline output

Scripts that already had partial output patterns (e.g., 40-AddedLSAProtection
with manual Json/Csv export) were refactored to use the v2 contract while
preserving their existing export behavior.

The 3 orchestration scripts (00-Copy-Local, 00-Run-Batch, 00-Run-Local) are
N/A for C9 and were not modified.

---

## Convention Compliance Summary (Post Phase 2.3)

| Convention | Before | After | Pass Rate |
|-----------|--------|-------|-----------|
| C1 v2 param contract | 51/51 | 51/51 | 100% |
| C2 v2-init block | 51/51 | 51/51 | 100% |
| C3 Bootstrap | 50/51 | 50/51 | 98% |
| C4 Module imports | 50/51 | 50/51 | 98% |
| C5 StrictMode Latest | 29/51 | 29/51 | 57% |
| C6 ErrorActionPreference Stop | 41/51 | **51/51** | **100%** |
| C7 Comment-based help | 49/51 | 49/51 | 96% |
| C8 ShouldProcess | 51/51 | 51/51 | 100% |
| C9 Output contract | 4/51 | **48/51** | **94%** |
| C10 Findings pattern | 23/51 | **24/51** | **47%** |
| C11 Exit code | 4/51 | **51/51** | **100%** |

> C5 (StrictMode) was not in scope for Phase 2.3.
> C10 remaining gaps require business logic changes (drift/notes to findings conversion).
> C9 3 N/A scripts are orchestration (00-Copy-Local, 00-Run-Batch, 00-Run-Local).

---

## Commits

1. `C6: add ErrorActionPreference Stop to 10 scripts` (10 files)
2. `C11: add exit 0 to 40 scripts` (40 files)
3. `C10: convert 40-AddedLSAProtection to New-FindingsList/Add-Finding` (1 file)
4. `C9: add v2 output contract to 44 scripts` (44 files)
