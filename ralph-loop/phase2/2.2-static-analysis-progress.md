# Phase 2.2 -- Static Analysis Fixes Progress

> Started: 2026-03-21

## Commit 1: High priority + StrictMode + verbose params + unused vars

| Finding | Status | Notes |
|---------|--------|-------|
| H1 | Fixed | Added `Set-StrictMode -Version Latest` to `scripts/06-UpdateHealth-SSU-Proof.ps1` |
| H2 | Fixed | Added `Set-StrictMode -Version Latest` to `scripts/11-IOC-Sweep-Defender.ps1` |
| H3 | Fixed | Added `Set-StrictMode -Version Latest` to `scripts/13-LSASS-CG-HVCI-VBS.ps1` |
| H4 | Fixed | Changed `$LASTEXITCODE -eq $null` to `$null -eq $LASTEXITCODE` in `scripts/00-Run-Profile.ps1` |
| H5 | Fixed | Changed 4 `-ForegroundColor` calls to `-Style` (Muted, Header, Warning) in `scripts/00-Run-Profile.ps1` |
| H6 | Fixed | Guarded `$InformationPreference = 'Continue'` with `if (-not $Quiet)` in `scripts/04-OfficeBrowser-Hardening-Proof.ps1` |
| M9 | Fixed | Removed `[Parameter(Mandatory = $false)]` in `lib/JsonCatalog.psm1` |
| M10 | Fixed | Removed 6 `[Parameter(Mandatory = $false)]` in `scripts/29-Network-Config-Audit.ps1` |
| M11 | Fixed | Removed 2 `[Parameter(Mandatory = $false)]` in `scripts/36-Backup-Readiness-Audit.ps1` |
| M12 | Fixed | Guarded `$InformationPreference` override in `scripts/12-Suspicious-Artifact-Grabber.ps1` |
| M13 | Fixed | Standardized 14 scripts from `Version 2.0` to `Latest`, 5 scripts from `Version 3.0` to `Latest` |
| M14 | Fixed | Added `Set-StrictMode -Version Latest` to `scripts/dev/fix-placeholders.ps1` and `scripts/dev/fix-error-handling.ps1` |
| PSScriptAnalyzer #1 | Fixed | `$eventLogReady` -> `$null = Ensure-EventSource` in `scripts/01-ASR-Defender-Allowlist.ps1` |
| PSScriptAnalyzer #2 | Fixed | `$canEventLog` -> `$null = Ensure-EventSource` in `scripts/20-MissingPatch-Notification.ps1` |

## Commit 2: Remove local function redefinitions (M2-M7)

| Finding | Status | Notes |
|---------|--------|-------|
| M2 (Test-IsAdmin) | Fixed | Removed from scripts 04, 05, 06, 07. All already import `Common.psm1`. |
| M3 (Ensure-Key) | Fixed | Removed from scripts 04, 05, 31, 39. Call-sites renamed to `Ensure-RegistryKey`. Added `Registry.psm1` import to script 05. |
| M4 (Ensure-Cmdlet) | Fixed | Removed from scripts 22, 23, 27, 29, 32, 44. Added `External.psm1` import to all six. |
| M5 (Ensure-Directory) | Partial | Removed from scripts 35, 42 (added `Common.psm1` import). Script 10 kept: different return-value semantics (returns `$true`/`$false`). |
| M6 (Ensure-DirectoryForFile) | Fixed | Removed from scripts 14, 20. Added `Common.psm1` import to both. |
| M7 (Get-FindingStats) | Fixed | Removed from script 33. `Console.psm1` already imported. |
| M8 (Get-StatusColor) | Skipped | Local versions in scripts 08, 17, 18, 23 have domain-specific status values incompatible with the lib version. Not a safe drop-in replacement. |

## Findings NOT addressed (deferred / out of scope)

| Finding | Reason |
|---------|--------|
| M1 (Write-ConsoleSummary) | 21 scripts have script-specific `Write-ConsoleSummary` with completely different signatures and logic from `lib/Console.psm1`. These are not true duplicates; they share a name but serve different purposes per script. Requires a design-level refactor. |
| M8 (Get-StatusColor) | 4 scripts have domain-specific color logic incompatible with the generic lib version. |
| L1-L13 | Low priority items deferred per severity ordering. |
| I1-I8 | Info items deferred per severity ordering. |

## Verification

- `verify.ps1`: Passed (PSScriptAnalyzer clean)
- Pester: 329 passed, 0 failed, 24 skipped
