{
  "name": "baselineops-windows",
  "version": "0.0.0",
  "private": true,
  "description": "Developer-only verification harness for BaselineOps for Windows.",
  "scripts": {
    "test": "node --test tests/fuzz/*.test.mjs",
    "fuzz": "node --test tests/fuzz/*.test.mjs"
  },
  "devDependencies": {
    "fast-check": "4.9.0"
  }
}
